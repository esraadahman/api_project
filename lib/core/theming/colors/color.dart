import 'dart:ui';

class colors{

//define colors using  hex codes

static const Color primary =Color.fromARGB(255, 47, 170, 45);
static const Color icons = Color.fromARGB(255, 4, 128, 2);
static const Color background =Color(0xffeaeef0);
static const Color sub_background = Color(0xfff3f4f6);

}